# Core data & dashboard
pandas>=1.5
numpy>=1.23
matplotlib>=3.6
plotly>=5.0
dash>=2.0
dash-bootstrap-components>=1.0
Pillow>=9.0

# Coordinate assignment (one-time setup only)
# pymupdf       # PDF → PNG rasterisation  (pip install pymupdf)
# opencv-python # Red-circle detection      (pip install opencv-python)
